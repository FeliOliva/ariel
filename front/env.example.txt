## Front (Create React App)
# IMPORTANTE: estas variables se inyectan en tiempo de build (npm run build / Docker build)
# En Docker: se pasa via build.args (ver front/docker-compose.yml)

# URL del backend
REACT_APP_API_URL=http://localhost:3001

